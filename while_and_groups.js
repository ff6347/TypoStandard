// written by fabiantheblind// http://www.the-moron.netvar 	d = app.documents.add();var 	pg = d.pages.add();var 	myY1 = 0;var 	myX1 = 0;var 	myY2 = d.documentPreferences.pageHeight;var 	myX2 = d.documentPreferences.pageWidth;var 	tf = pg.textFrames.add();		//set the position of the text		tf.geometricBounds = [myY1, myX1, myY2/3, myX2];		tf.contents = TextFrameContents.placeholderText; 				tf = pg.textFrames.add();		//set the position of the text		tf.geometricBounds = [myY1+(d.documentPreferences.pageHeight/3), myX1, myY2		-(d.documentPreferences.pageHeight/3), myX2];		tf.contents = TextFrameContents.placeholderText;				tf = pg.textFrames.add();		//set the position of the text		tf.geometricBounds = [myY1+((d.documentPreferences.pageHeight/3)*2), myX1, myY2, myX2];		tf.contents = TextFrameContents.placeholderText;				var		i = 0; // could also be myInt but to use i is an programming syntax conventionvar 	myArray = new Array();				while ( i <= 2 )		{		myArray.push(pg.textFrames.item(i));		i++;		}				pg.groups.add(myArray);				alert("Operation Complete!");