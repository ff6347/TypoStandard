// basic javascript snippets for indesign cs 4// written by fabiantheblind// first of all open basicDoc.idml or use script buildDocument.jsx// to build the doc we are using//this runs the functionmain();// this is the functionfunction main(){// setup some variablesvar theDoc = app.activeDocument; // active docvar pg = theDoc.pages.item(0); // first pagevar tf = pg.textFrames.item(0); // the one textframe that is there// now we can loop thru the textvar pars = tf.paragraphs;for(var i = 0;i < pars.length; i++){	var val = Math.random()*23;	var p  = pars[i];		if(val < 4.9){			val = 5 + Math.random()*5;			}	// the next line loops thru all characters in the text	p.pointSize = val;	// or use	// app.activeDocument.pages.item(0).textFrames.item(0).paragraphs.item(i).pointSize = val;	}}