// written by fabiantheblind// http://www.the-moron.net// create a documentvar doc = app.documents.add();// a new document always has a pagevar thePage = doc.pages.item(0);// or use// var thePage = doc.pages[0];// create a textFrame on the first pagevar textBox = thePage.textFrames.add();// get the documents margin prefrences of the docvar mp = doc.marginPreferences;// get the docuements preferencesvar dp = doc.documentPreferences;// define the bounds for the textframevar y1 = mp.top;var x1 = mp.left;var y2 = dp.pageHeight - mp.bottom;var x2 = dp.pageWidth - mp.right;// set the textframes geometricBoundstextBox.geometricBounds = [y1,x1,y2,x2];// add some content to the textframetextBox.contents = "Hello World";// set the fontSizetextBox.characters.everyItem().pointSize = 23;// set the fonttextBox.characters.everyItem().appliedFont = "Arial";// get all the characters in the textFramec = textBox.characters.everyItem();// tis is a loopfor(var i = 0; i < c.length;i++ ){	// do something in here		}