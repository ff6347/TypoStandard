// basic javascript snippets for indesign cs 4// written by fabiantheblind// first of all open basicDoc.idml or use script buildDocument.jsx// to build the doc we are using// what are functions?//this runs a functionmain();// this is the functionfunction main(){// setup some variablesvar d = app.documents.add(); // active docvar pg = d.pages.item(0); // first pagevar tf = pg.textFrames.add(); // the one textframe that is there// the function buildBounds(app.documents.item());// calculates the size of the textframetf.geometricBounds = buildBounds(d);tf.contents = "Hello World";// now we can loop thru the textfor(var i = 0;i < tf.characters.length; i++){	var c = tf.characters[i];	// the next line loops thru all characters in the text	c.pointSize = calcVal(23);	}}// this is a function that gets called// and returns a valuefunction calcVal(value){		var val = Math.random()*value;		if(val < 4.9){			val = 5;			}	return val;		}function buildBounds(doc){	// calculate the position of the text frame	var w = doc.documentPreferences.pageWidth;	var h = doc.documentPreferences.pageHeight;	var theSize =  100;	var Y1 = (h/2)-(theSize/2);	var X1 = (w/2)-(theSize/2);	var Y2 = Y1 + theSize;	var X2 = X1 + theSize;	return [Y1,X1,Y2,X2];	}